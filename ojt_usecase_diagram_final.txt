graph TD
    %% Actors
    Admin((Admin))
    Supervisor((Supervisor))
    Student((Student))

    %% Use Cases
    subgraph "Authentication"
        AL[Login as Admin]
        SL[Login as Supervisor]
        STL[Login as Student]
    end

    subgraph "Admin Functions"
        AS[Add Supervisor]
        MS[Manage Supervisors]
        UF[Upload Files]
    end

    subgraph "Supervisor Functions"
        AST[Add Student]
        CP[Create Project]
        MP[Manage Projects]
        UD[Upload Documents]
        MA[Mark Student Absent]
        VA[View Attendance]
        VATT[Verify Attendance]
        ES[Evaluate Student]
        GC[Generate Certificate]
    end

    subgraph "Student Functions"
        RT[Record Daily Task]
        TIO[Time In/Out]
        VAH[View Attendance History]
        EA[Export Attendance]
        VP[View Projects]
        SP[Submit Project]
        VSUB[View Submissions]
        VE[View Evaluation]
    end

    subgraph "Project Management"
        RS[Review Submissions]
        VSUB2[View Submission]
        DF[Download File]
        GS[Grade Submission]
        APS[Approve Submission]
        RJS[Reject Submission]
    end

    subgraph "Certificate Process"
        CE[Check Eligibility]
        ASIG[Add Signature]
        GPDF[Generate PDF]
        DC[Download Certificate]
    end

    subgraph "Document Management"
        VPDF[View PDF]
        DD[Delete Document]
    end

    %% Relationships
    Admin --> AL
    Supervisor --> SL
    Student --> STL

    AL --> AS
    AL --> MS
    AL --> VS
    AL --> UF

    SL --> AST
    SL --> CP
    SL --> MP
    SL --> UD
    SL --> MA
    SL --> VA
    SL --> VATT
    SL --> ES
    SL --> GC

    STL --> RT
    STL --> TIO
    STL --> VAH
    STL --> EA
    STL --> VP
    STL --> SP
    STL --> VSUB
    STL --> VE

    MP --> RS
    RS --> VSUB2
    RS --> DF
    RS --> GS
    GS --> APS
    GS --> RJS

    ES --> CE
    CE --> GPDF
    GPDF --> ASIG
    ASIG --> DC

    UD --> VPDF
    VPDF --> DD

    SP --> RS

    %% Styling
    classDef actorClass fill:#e1f5fe,stroke:#01579b,stroke-width:2px
    classDef usecaseClass fill:#f3e5f5,stroke:#4a148c,stroke-width:2px

    class Admin,Supervisor,Student actorClass
    class AL,SL,STL,AS,MS,VS,UF,AST,CP,MP,UD,MA,VA,VATT,ES,GC,RT,TIO,VAH,EA,VP,SP,VSUB,VE,RS,VSUB2,DF,GS,APS,RJS,CE,GPDF,ASIG,DC,VPDF,DD usecaseClass
